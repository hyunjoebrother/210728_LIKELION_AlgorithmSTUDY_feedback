// 문제 4. 더 맵게 - Heap

// feedback - 1 (PSJ)
// 최소 heap으로 전체의 최솟값인 루트의 값이 K 이상될 때까지 계속 섞음
// 루트 값이 K 이상이 되지 못했는데 heap에 루트만 남으면 -1 return

#include <string>
#include <vector>

using namespace std;

int solution(vector<int> scoville, int K) {
    int answer = 0;
    return answer;
}